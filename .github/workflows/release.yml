name: Build and Release

on:
  push:
    branches:
      - master

jobs:
  build-and-release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0'

      - name: Restore dependencies
        run: dotnet restore

      - name: Get Git commit hash
        id: get_version
        run: |
          $hash = "${{ github.sha }}".Substring(0, 7)
          echo "version=$hash" >> $env:GITHUB_OUTPUT
          echo "Found commit hash: $hash"
        shell: powershell

      - name: Build single-file for Windows x64
        run: |
          $hash = "${{ steps.get_version.outputs.version }}"
          dotnet publish -c Release -r win-x64 --self-contained true `
            -p:PublishSingleFile=true `
            -p:DebugType=none `
            -p:EnableCompressionInSingleFile=true `
            -p:Version=$hash `
            -p:FileVersion=$hash `
            -p:InformationalVersion=$hash `
            -p:AssemblyVersion=$hash `
            -o ./publish/win-x64 XOSC.csproj
        shell: powershell

      - name: Build single-file for Linux x64
        run: |
          $hash = "${{ steps.get_version.outputs.version }}"
          dotnet publish -c Release -r linux-x64 --self-contained true `
            -p:PublishSingleFile=true `
            -p:DebugType=none `
            -p:EnableCompressionInSingleFile=true `
            -p:Version=$hash `
            -p:FileVersion=$hash `
            -p:InformationalVersion=$hash `
            -p:AssemblyVersion=$hash `
            -o ./publish/linux-x64 XOSC.csproj
        shell: powershell

      - name: Create ZIP archive with both platforms
        run: |
          Compress-Archive -Path ./publish/* -DestinationPath XOSC.zip
          Write-Host "Created XOSC.zip with single-file executables"
        shell: powershell

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: ./XOSC.zip
          tag: v${{ steps.get_version.outputs.version }}
          name: v${{ steps.get_version.outputs.version }}
          prerelease: false
          allowUpdates: true
          makeLatest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}